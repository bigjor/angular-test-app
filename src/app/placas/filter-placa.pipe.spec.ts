import { FilterPlacaPipe } from './filter-placa.pipe';

describe('FilterPlacaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlacaPipe();
    expect(pipe).toBeTruthy();
  });
});
